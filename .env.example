# ===================== 环境变量示例 =====================
# 复制本文件为 .env（docker compose 会自动读取），并填入真实值：
#   cp .env.example .env
# 注意：.env 已被 git 忽略，切勿提交真实密钥到仓库。

# 管理员引导密钥（必填，无默认值）
# 用于首次创建管理员账号（POST /api/v1/admin/register）。
# 请使用足够随机的高强度值，例如：
#   openssl rand -hex 32
ADMIN_SETUP_SECRET=replace-with-a-strong-random-string

# Redis 访问密码（必填，无默认值；本地开发不部署 Redis 时可留空）
REDIS_PASSWORD=replace-with-a-strong-random-string

# Redis 地址（默认 localhost；docker compose 部署时由服务编排覆盖为 redis）
# REDIS_HOST=localhost

# 数据库文件路径（默认 ./chat_server_db.sqlite）
# DB_PATH=chat_server_db.sqlite
